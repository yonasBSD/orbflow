{
  "private": true,
  "packageManager": "pnpm@10.30.2",
  "scripts": {
    "dev": "turbo dev --filter=orbflow-web",
    "build": "turbo build",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.9.14",
    "vitest": "^4.1.0"
  },
  "pnpm": {
    "overrides": {
      "zustand": "^5.0.0"
    }
  }
}
