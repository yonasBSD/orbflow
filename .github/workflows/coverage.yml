name: Coverage

on:
  push:
    branches: [main]
    paths: ["crates/**", "Cargo.toml", "Cargo.lock"]
  pull_request:
    paths: ["crates/**", "Cargo.toml", "Cargo.lock"]

permissions:
  contents: read

jobs:
  rust-coverage:
    name: Rust Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: dtolnay/rust-toolchain@4305ee97f1d8ddd902f2f5a3fcb6e6f62bcfbfb8 # stable
      - uses: Swatinem/rust-cache@9d47c6ad4b02e050fd481d890b2ea34778fd09d6 # v2.7.8

      - name: Install cargo-tarpaulin
        run: cargo install cargo-tarpaulin

      - name: Generate coverage
        run: cargo tarpaulin --workspace --out xml --output-dir coverage --timeout 300 --skip-clean

      - name: Upload to Codecov
        uses: codecov/codecov-action@18283e04ce6e62d37312384ff67ab1fd00d3ead9 # v5.4.3
        with:
          files: coverage/cobertura.xml
          flags: rust
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}
